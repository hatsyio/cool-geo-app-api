with reduced_postal_codes as (
    select id, code
    from postal_codes
)
select {{ select_fields }} coalesce(sum(amount), 0) as amount
from paystats p
    inner join reduced_postal_codes pc on pc.id = p.postal_code_id
where 1=1
    {{ where_clause }}
{{ group_by }}
;
